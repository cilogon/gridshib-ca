<html>
<head>
<!-- $Id$ -->
<title>GridShib CA: ChangeLog</title>
<!-- global style sheet -->
<link rel="stylesheet" type="text/css" href="/styles/global.css" />

<!-- local style sheet -->
<link rel="stylesheet" type="text/css" href="/styles/local.css" />
</head>
@DOC_HEADER@

<b>Version @PACKAGE_VERSION@:</b>
<ul>
<li>CredentialRetriever now using HTTP status code instead of own
	internal status header.
<li>CredentialRetriever now generating key pair and sending
	certificate request to generateCred.pl
<li>Added Command.pm to handle executing programs.
<li>Now builds and installs own openssl-based ca (openssl-ca)
	that allows setting of DN by GridShib CA
<li>Added CA.pm as base interface for CA interactions with
	OpenSSLCA.pm as an instantiation for local OpenSSL-based CA
<li>Starting to use RFC 2253 style DNs internally.
<li>CredentialRetreiver.java: Circumvent JWS installing its own
	SSLSocketFactory so that default SSLSocketFactory is used and our
	list of trusted CAs is honored.
</ul>

<b>Version 0.2.0:</b> Changelog started Oct 7, 2006

@DOC_FOOTER@
</html>
